package util;import org.openqa.selenium.WebDriver;import org.openqa.selenium.firefox.FirefoxDriver;public class DriverUtil {    public static WebDriver driver; // this is an object    public void setdrivers(String browser) { // this is a method to set the drivers        if (driver == null) {            if (browser.equalsIgnoreCase("firefox")) ;            System.setProperty("webdriver.gecko.driver", "/Users/admin/Documents/Drivers/geckodriver");            driver = new FirefoxDriver();        }    }    public void tearDown() {        driver.quit();        driver = null;    }}